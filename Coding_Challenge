# Write a function that returns the most common letter in a string.
def common_letter(string):
    most = ""
    for letter in string:
        if most == "" or string.count(letter) > string.count(most):
            most = letter
    return most

string = input("Enter a String: ")
print(common_letter(string))